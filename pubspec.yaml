name: flutter_floating_particles
description: A highly customizable, performant Flutter package for dynamic animated particle effects. Add stars, snowflakes, dots, or custom shapes as interactive backgrounds and overlays.
version: 1.0.0
homepage: https://github.com/nitesh695/flutter_floating_particles
repository: https://github.com/nitesh695/flutter_floating_particles
issue_tracker: https://github.com/nitesh695/flutter_floating_particles/issues

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

license: MIT

